<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grey"
    tools:context=".SecondActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/tb_main"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="match_parent"
        android:minHeight="?attr/actionBarSize"
        android:layout_alignParentLeft="true"
        android:layout_margin="8dp"
        android:elevation="4dp"
        android:background="@drawable/shadow"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Digite o IP"
        android:textColor="@color/black"
        android:id="@+id/textView2"
        android:layout_below="@+id/tb_main"
        android:layout_alignLeft="@+id/txtIp"
        android:layout_alignStart="@+id/txtIp" />

    <include
        android:id="@+id/inc_tb_bottom"
        layout="@layout/toolbar_bottom"/>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/txtIp"
        android:layout_below="@+id/textView2"
        android:layout_alignLeft="@+id/tb_main"
        android:layout_alignStart="@+id/tb_main"
        android:layout_alignRight="@+id/tb_main"
        android:layout_alignEnd="@+id/tb_main"
        android:layout_marginRight="253dp"
        android:layout_marginEnd="253dp"
        android:inputType="number"
        android:autoText="false" />

</RelativeLayout>

